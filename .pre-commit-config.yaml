# install:
#  pip3 install pre-commit
# run:
#  pre-commit run -a
# update deps:
#  pre-commit autoupdate

repos:
  - repo: https://github.com/henryykt/pre-commit-perl
    rev: v0.0.5
    hooks:
      # - id: perlcritic
      - id: perltidy
      # - id: perlimports
